## Handoff
- Owner: Codex + Antigravity UI workstream
- Goal: Enforce the approved three-word/two-sentence candidate contract, restore the `WorksheetCharacter` font switch, add final-preview candidate replacement controls, and add table-safe Tian-grid guides.
- Changed files: `src/domain/analysis-result.ts`, `src/domain/index.ts`, `src/services/use-cases.ts`, `src/services/worksheet-builder.ts`, `src/services/reference-template-docx.ts`, `src/assets/docx-templates/生字學習單注音版.docx`, `src/features/preview/PrintPreviewPage.tsx`, `src/features/preview/candidate-selection.ts`, `src/features/review/ReviewPage.tsx`, `src/styles/app.css`, `tests/moe-dictionary.test.ts`, `tests/review-status-removal.test.ts`, `tests/reference-template-docx.test.ts`, `tests/preview-candidate-selection.test.ts`, `PROJECT.md`, `docs/word-template-design-guide.md`
- Dictionary version: unchanged
- Contract change: `AnalysisResult.characters[].wordCandidates` is canonicalized to at most 3 entries and `sentenceCandidates` to at most 2 entries by the Zod schema. Initial analysis, teacher confirmation, worksheet construction, and easy-template-x data assembly all enforce the same limits. The full dictionary lookup remains unchanged and is used only as the teacher-controlled replacement pool.
- Verified: Grade 1 and 2 resolve to `zihi-kai-zhuyin`; grade 3 and above resolve to `standard-kai`; an explicit export override wins. The template contains exactly three `{character}` runs with `w:rStyle="WorksheetCharacter"` and no direct `w:rFonts`. A forced grade-6 export with `{ font: 'zihi-kai-zhuyin' }` produced three styled output runs, an IVS character value, and `word/styles.xml` with `w:eastAsia="ㄅ字嗨注音標楷 Regular"`. Microsoft Word rendered the forced output with visible character-plus-zhuyin glyphs and the table-safe dashed Tian-grid guides. Full test, lint, and production-build results are recorded in the completion response.
- Risks / open questions: The Tian grid is constructed from ordinary overlaid dashed glyph runs rather than drawings, so it remains safe inside the easy-template-x `{#items}` loop. Its appearance depends on Word's `Segoe UI Symbol`, which is standard on supported Windows systems. The candidate replacement algorithm does not rank commonness because the dictionary has no frequency field.
- Next owner action: Review the supplied forced-zhuyin DOCX/PDF at 100% zoom and confirm the dashed guide color and length. If a different batch-selection policy is desired, define it without inferring word frequency.
