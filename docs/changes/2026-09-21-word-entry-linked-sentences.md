## Handoff
- Owner: Codex + Antigravity UI workstream
- Goal: Preserve each MOE dictionary word entry's own examples and make initial, review, preview, and replacement sentence selection follow the teacher's currently selected words before using the overall related-entry fallback pool.
- Changed files: `src/infrastructure/moe-dictionary.ts`, `src/infrastructure/index.ts`, `src/services/use-cases.ts`, `src/services/index.ts`, `src/features/preview/PrintPreviewPage.tsx`, `src/features/review/ReviewPage.tsx`, `tests/moe-dictionary.test.ts`, `tests/preview-candidate-selection.test.ts`, `PROJECT.md`, `docs/word-template-design-guide.md`, `docs/changes/2026-09-21-word-entry-linked-sentences.md`
- Dictionary version: unchanged (`2014_20260626`)
- Contract change: `AnalysisResult.wordCandidates` remains `string[]`; its shape did not change. `CharacterDictionaryLookup` adds `wordCandidateDetails: Array<{ text: string; zhuyin: string; entryWordNumber: string; sentenceCandidates: string[]; source: '教育部《國語辭典簡編本》' }>` and services export `resolveSentenceCandidatesForWords(lookup, selectedWords)`. The helper returns the de-duplicated examples belonging to the selected word entries, or the existing overall `lookup.sentenceCandidates` pool only when no selected entry supplies an example.
- Verified: Dictionary tests confirm entry word numbers and per-entry examples are preserved, selected-word examples exclude unrelated entries, entries without examples trigger the overall fallback, and initial analysis stores at most two linked examples. Preview tests confirm sentence replacement uses the linked pool and falls back for unmatched teacher-entered words. A real sentence-practice DOCX was opened by Microsoft Word, exported to PDF, rasterized, and visually inspected; it displayed the selected words `博學、博士學位、拜師學藝` with examples sourced from those entries. Full test, lint, and production-build results are recorded in the completion response.
- Risks / open questions: The dictionary provides no frequency or commonness field, so the implementation preserves source order and never ranks candidates. Multiple dictionary entries with the same displayed word remain separate in `wordCandidateDetails`; their examples are merged when that displayed word is selected. `wordCandidateDetails` is a lookup-only pool and is intentionally not persisted into `AnalysisResult` or exposed as a Word template tag.
- Next owner action: Review the supplied DOCX/PDF and confirm whether changing words should always overwrite manually edited sentences; the current preview batch action refreshes linked sentences, while review-page manual sentence text remains under teacher control until they request another sentence.
